// ✅ نسخة آمنة تعمل على GitHub Pages و PWA
// ❌ بدون Gemini SDK
// ❌ بدون API KEY

const delay = (ms: number) => new Promise(res => setTimeout(res, ms));

export const getStudyTips = async (topic: string): Promise<string> => {
  await delay(800); // إحساس AI حقيقي 😄

  return `
📚 نصائح ذهبية لمذاكرة ${topic}:

1️⃣ قسم الدرس لأجزاء صغيرة وذاكر جزءًا جزءًا  
2️⃣ استخدم ورقة وقلم واكتب بيدك – ده بيثبت المعلومة  
3️⃣ راجع قبل النوم ولو 5 دقائق 🌙  

🔥 كمل يا بطل… أنت أقرب للنجاح مما تتخيل!
`;
};

export const explainTerm = async (term: string): Promise<string> => {
  await delay(600);

  return `
"${term}" هو مصطلح تعليمي يُقصد به معنى أو فكرة يتم استخدامها في الدراسة،
وتم شرحه هنا بطريقة مبسطة لتسهيل الفهم على الطالب ✅
`;
};

